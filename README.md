# Seven INC

## How to run:

- Clone the project

- Install the dependencies
> In the terminal of the application, run the command:
> ```bash
> npm i
>```

- Run the project
> Now run the command
> ```bash
> npm run serve
>```
